<?php
    // if( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) exit();
    // global $wpdb;
    // $wpdb->query( "DROP TABLE IF EXISTS {$wpdb->prefix}blck_payments;DROP TABLE IF EXISTS {$wpdb->prefix}block_api; DROP TABLE IF EXISTS {$wpdb->prefix}pay_to_unlock" );
    // delete_option("my_plugin_db_version");
?>